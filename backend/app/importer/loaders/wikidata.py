"""Wikidata loader — STUB pending Phase C/D.

Top-of-ticket sanity check only. Pull via SPARQL endpoint
(https://query.wikidata.org/sparql) for "Q...Nigerian general election".
"""

from __future__ import annotations

import logging

log = logging.getLogger(__name__)


def load_wikidata(*args, **kwargs):  # type: ignore[no-untyped-def]
    raise NotImplementedError("Wikidata loader is a Phase C/D stub.")
